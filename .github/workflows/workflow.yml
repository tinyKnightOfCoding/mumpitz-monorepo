name: workflow.yml
on:
  push:
    branches:
      - main
jobs:
  workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: pnpm/setup@v2
        with:
          cache: true
      - run: |
          node -v
          pnpm -v
