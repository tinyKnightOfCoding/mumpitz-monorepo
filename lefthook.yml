output:
  - summary
  - meta
pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{js,ts,jsx,tsx,mjs,mts,cjs,cts,json,yml,yaml,md}'
      run: pnpm --silent oxfmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
